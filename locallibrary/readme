To run this project
1. Open virtual environment 
    1.1 In command prompt enter 'mkvirtualenv my_django_environment'
2. If django is not imported. (Check version 'py -m django --version')
    2.2 Install django from pip 'pip3 install django'
3. Run server
    3.3 py manage.py runserver

If migration is needed.
py manage.py makemigrations
py manage.py migrate

1. workon my_django_environment
2. cd C:\Django_test\django_local_library\locallibrary

2024.05.26


How to build user creating method.
Migrating my project to ec2 ubuntu server instance.
GIT connection availability